# protease-bots

Image transformation through mathematical distortion.

Named after protease — the enzyme named after Proteus, the Greek sea god who took any form to resist capture. These bots warp, cleave, and recompose images through conformal maps, coordinate transforms, and mathematical distortions.

## Bots

*(coming soon)*

## Setup

```bash
pip install -r requirements.txt
source ~/.config/secrets/au-slack.env
```

## Shared utilities

- `slack_fetcher.py` — fetch random images from a Slack channel
- `slack_poster.py` — post images to a Slack channel
